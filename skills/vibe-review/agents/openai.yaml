interface:
  display_name: "Vibe Review"
  short_description: "Review diffs separately along standards and spec axes"
  default_prompt: "Use $vibe-review to review changes after a fixed point against repository standards and source specs."

policy:
  allow_implicit_invocation: true
